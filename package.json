{
  "name": "dsh-api-ledger",
  "version": "0.2.1",
  "private": true,
  "description": "DSH 插件：按 API 凭据统计 token 与费用，不同额度池分列不合并。Per-credential token and cost ledger for DeepSeek Harness.",
  "type": "module",
  "license": "MIT",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./client": "./lib/client.js",
    "./package.json": "./package.json"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md"
  ],
  "scripts": {
    "check": "node --check lib/index.js && node --check lib/pricing.js && node --check lib/identities.js && node --check lib/records.js && node --check lib/client.js && node --check lib/config.js && node --check scripts/install.mjs",
    "test": "node tests/pricing.test.js && node tests/identities.test.js && node tests/records.test.js && node tests/i18n.test.js && node tests/report.test.js && node tests/client-wiring.test.js && node tests/client-behavior.test.js && node tests/config.test.js"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web",
      "inject": [
        "@deepseek-ai/dsh-client-locale",
        "@deepseek-ai/dsh-client-ui-conversation",
        "@deepseek-ai/dsh-client-ui-settings",
        "@deepseek-ai/dsh-client-ui-sidebar"
      ]
    }
  }
}
